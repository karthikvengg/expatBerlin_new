import { PlaceholderPage } from "../components/PlaceholderPage";

export function WorkPermits() {
  return (
    <PlaceholderPage
      titleKey="workPermitsTitle"
      descriptionKey="workPermitsDesc"
    />
  );
}
